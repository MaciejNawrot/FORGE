# Shared/root environment variables.
# Each app also has its own .env.example (apps/web/.env.example, apps/api/.env.example) —
# copy those to .env in the respective app directory too.

NODE_ENV=development
