{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tags": ["ui"],
  "tasks": {
    "build-storybook": {
      "dependsOn": ["^build"],
      "outputs": ["storybook-static/**"]
    }
  }
}
